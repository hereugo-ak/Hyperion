# README visual assets — validation notes

## Architecture diagram

The revised `hyperion-system-architecture.png` presents the runtime in three reading-order planes: control, evidence and intelligence, and assurance and delivery. It also exposes model routing and run journaling as a compact platform foundation. The visible labels accurately reflect the corresponding implementation contracts: corpus preflight; evidence-led retrieval; eligibility-gated specialist research; synthesis and fact checking; the 10-dimension quality gate; visualisation, design, and rendering; model routing; and durable replay.

## Engagement lifecycle diagram

The initial lifecycle diagram rendered as a very wide, shallow image. It is structurally sound, but it needs a compact re-layout before inclusion to remain legible when GitHub constrains README width. The revised asset should retain the code-accurate branches for RED corpus termination, GREEN/AMBER continuation, quality-driven revision, and retained run artifacts.

## Brand treatment

Both visual sources use a deterministic dark-canvas palette compatible with the Hyperion terminal heading: warm clay for control, green for evidence, blue for analysis, rose for assurance/delivery, and muted stone for persisted artifacts. The diagrams are rendered from source-controlled Mermaid files and should remain coupled to the implementation rather than serving as decorative illustrations.
